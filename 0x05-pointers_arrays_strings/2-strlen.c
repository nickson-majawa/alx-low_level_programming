#include "holberton.h"
/**
 * _strlen - returns the length of a string
 * @s: the character to measure the length on
 * Return: returns length
 */
int _strlen(char *s)
{
*s = "Holberton";
len = strlen(s);
return (len);
}
