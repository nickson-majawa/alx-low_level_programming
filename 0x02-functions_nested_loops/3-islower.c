#include "holberton.h"
/**
 *_islower = checks for lowercase alphabet letters
 *@c: the lowercase letter to check
 * Return: returns the lowercase letters
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
