#include "holberton.h"
/**
 * _abs - finds the absolute value of a given integer
 * @n: the int value to check
 * Return: returns the absolute value
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
