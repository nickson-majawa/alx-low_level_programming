#include "holberton.h"
/**
 * mul - multiplies 2 integers a and b
 * @a: paramater value used for multiplication
 * @b: other parameter value used for multiplication
 * Return: return multiplication result
 */
int mul(int a, int b)
{
int result;
result = a * b;
return (result);
}
